#!/usr/bin/env python

import os
import sys
from setuptools import setup
 
setup(
     name='aspy',    # This is the name of your PyPI-package.
     packages = ['aspy'],
     version='0.1',                          # Update the version number for new releases
)
